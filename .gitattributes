# Generated by `npm run sync:bios`. With more than one person syncing, both
# will regenerate this file and git will try to merge two machines' output
# line by line — which can produce syntactically broken JSON that still looks
# plausible in a diff.
#
# `-merge` makes git refuse to auto-merge and flag the whole file instead. The
# resolution is never to hand-edit it: take either side and re-run the sync,
# which reproduces the current state of Notion deterministically (keys sorted).
#
#   git checkout --ours lib/team-bios.generated.json
#   npm run sync:bios
#   git add lib/team-bios.generated.json
lib/team-bios.generated.json -merge

# Generated by `npm run sync:live-type` from the live site. Deterministic
# (keys sorted), so the resolution for a conflict is to take either side and
# re-run the sync rather than hand-merge it.
design-system/live-type.generated.json -merge

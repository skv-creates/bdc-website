# Cloudflare Workers static-assets headers.
# The /_img variants are content-addressed by their source: a picture is
# replaced by adding a file under a new name (event sync writes
# slug-numbered files), so the URLs are safe to cache forever.
/_img/*
  Cache-Control: public, max-age=31536000, immutable

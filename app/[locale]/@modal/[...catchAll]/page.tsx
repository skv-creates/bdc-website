// Closes the intercepted overlay on client-side navigation to any route the
// @modal slot doesn't explicitly match (parallel slots otherwise keep their
// last active subpage visible). See Next.js parallel-routes "modals" docs.
export default function CatchAll() {
  return null;
}
